*zj-theme.txt*                       Sync zellij's theme with your colorscheme
*zj-theme*

==============================================================================
CONTENTS                                                   *zj-theme-contents*

  Introduction ....................................... |zj-theme-introduction|
  Requirements ....................................... |zj-theme-requirements|
  Installation ....................................... |zj-theme-installation|
  Configuration ..................................... |zj-theme-configuration|
  Commands ............................................... |zj-theme-commands|
  Built-in mappings ...................................... |zj-theme-mappings|
  Fallback behavior ...................................... |zj-theme-fallback|
  Pane backgrounds ................................ |zj-theme-pane-backgrounds|
  Health ................................................... |zj-theme-health|

==============================================================================
INTRODUCTION                                           *zj-theme-introduction*

zj-theme.nvim syncs zellij's theme to whatever nvim colorscheme is
currently active.

It rewrites zellij's config file directly. zellij watches that file for
changes and applies them to your already-running session automatically
(zellij polls it roughly once a second), so this takes effect live — no
restart needed.

How it works:

  1. A |ColorScheme| autocmd fires whenever you run `:colorscheme ...` (or a
     plugin sets one on your behalf).
  2. The new `vim.g.colors_name` is looked up in a mapping table (built-in
     defaults + anything you add) to find the corresponding zellij theme
     name.
  3. The `theme "..."` line in zellij's `config.kdl` is rewritten in place
     with that theme name.
  4. If any step fails — you're not inside a zellij session, the colorscheme
     has no mapping, the config file isn't readable, or it has no `theme`
     line to rewrite — it falls back to a configurable default zellij theme
     instead of leaving things inconsistent.

==============================================================================
REQUIREMENTS                                           *zj-theme-requirements*

Neovim >= 0.10, for the `vim.health.*` API used by |zj-theme-health|.

No external plugin dependency needed — see |zj-theme-pane-backgrounds| for
how nvim's own pane and every other pane get synced. Syncing backgrounds to
every other pane needs zellij >= 0.44.0 and the `zellij` CLI on `PATH`;
syncing nvim's own pane has no such requirement, just a terminal that
understands OSC 11/12 (most modern terminals do — alacritty, foot,
gnome-terminal, kitty, wezterm, st, and others like them).

==============================================================================
INSTALLATION                                           *zj-theme-installation*

No external plugin dependency needed — every install snippet below is
just this plugin on its own.

Works with any plugin manager. Example with lazy.nvim:
>lua
  {
    "jaimeibanezrivera/zj-theme",
    lazy = false,
    config = function()
      require("zj-theme").setup({
        -- see |zj-theme-configuration|
      })
    end,
  }
<
See the README for packer.nvim, vim-plug, mini.deps, and vim.pack examples.

==============================================================================
CONFIGURATION                                         *zj-theme-configuration*
>lua
  require("zj-theme").setup({
    -- Path to zellij's config.kdl. Defaults to ~/.config/zellij/config.kdl.
    zellij_config_path = vim.fn.expand("~/.config/zellij/config.kdl"),

    -- zellij themes to fall back to when the current colorscheme has no
    -- mapping, or when writing zellij_config_path fails. Picked based on
    -- vim.o.background so a dark colorscheme doesn't fall back to a light
    -- zellij theme or vice versa.
    default_dark_theme = "default",
    default_light_theme = "pencil-light",

    -- nvim colors_name -> zellij theme name. Merged over the built-in table
    -- in lua/zj-theme/mappings.lua, so you only need
    -- overrides/additions. A value can also be { dark = "...", light = "..." }
    -- for colorschemes that keep vim.g.colors_name the same across
    -- light/dark (ayu.nvim, gruvbox-material, everforest-nvim all do this,
    -- and are already handled by the bundled defaults) — resolved via the
    -- current vim.o.background.
    mappings = {
      mycustomtheme = "my-zellij-theme-name",
      myflexibletheme = { dark = "my-dark-zellij-theme", light = "my-light-zellij-theme" },
    },

    -- Whether to push the current colorscheme's bg/fg to every pane in the
    -- zellij session: the pane nvim itself is running in, and every other
    -- pane. See |zj-theme-pane-backgrounds|. Set to false to leave every
    -- pane's background alone.
    sync_pane_backgrounds = true,

    -- How often (ms) to poll for newly created zellij panes and color them
    -- to match the current colorscheme — otherwise a pane opened after a
    -- `:colorscheme` switch keeps zellij's default background until the
    -- next one. Set to 0 to disable polling.
    pane_poll_interval_ms = 1000,

    -- Set to false to silence vim.notify warnings/errors.
    notify = true,
  })
<
Your `config.kdl` needs an actual (uncommented) `theme "..."` line already
present for this plugin to find and rewrite — it won't add one from
scratch.

==============================================================================
COMMANDS                                                   *zj-theme-commands*

                                                                  *:ZjThemeNow*
:ZjThemeNow
  Re-applies the mapping for whatever colorscheme is currently active —
  useful for testing config changes without switching colorschemes.

==============================================================================
BUILT-IN MAPPINGS                                          *zj-theme-mappings*

See `lua/zj-theme/mappings.lua` for the full list (catppuccin,
gruvbox, tokyonight, nord, dracula, kanagawa, everforest, solarized, ayu,
onedark, and a few variants). These are best-effort — override any of them
via `mappings` in |zj-theme-configuration|.

==============================================================================
FALLBACK BEHAVIOR                                          *zj-theme-fallback*

`default_dark_theme` or `default_light_theme` (chosen by `vim.o.background`)
is used whenever:

  - nvim is not running inside a zellij session (`$ZELLIJ` unset) — no-op,
    nothing is written at all.
  - the active colorscheme has no entry in `mappings`.
  - `zellij_config_path` doesn't exist/isn't readable, or has no
    `theme "..."` line to rewrite.

==============================================================================
PANE BACKGROUNDS                                   *zj-theme-pane-backgrounds*

Rewriting zellij's theme only changes zellij's own colors — borders, tab
bar, status bar. It doesn't touch the background you actually see inside
each pane. zj-theme.nvim handles that itself, both covered by the single
`sync_pane_backgrounds` option, via two different mechanisms under the
hood:

  - The pane nvim is running in gets its background and cursor color set
    directly, via raw terminal escape sequences (OSC 11/12) — no separate
    plugin needed, and no `zellij` CLI or version requirement; this part
    keeps working even if that's missing.
  - Every other pane in the session gets recolored using a zellij command
    that can set a pane's background directly (needs zellij 0.44 or
    newer). This part needs the `zellij` command to actually be available
    on your system, not just a zellij session to be running —
    `:checkhealth zj-theme` will tell you if it isn't.

Colors are never reset back to zellij's own defaults — once a pane is
colored it stays that way, including after nvim exits, until the next
`:colorscheme` switch recolors it again. The goal is a session that keeps
looking like your last active colorscheme everywhere, not just while nvim
happens to be running in one of its panes.

Set `sync_pane_backgrounds = false` to turn off both.

See the demo GIF in the README (assets/demo-panes.gif) for this in action.

One call at a time: under the hood, this plugin never asks zellij to
recolor several panes at once — it queues the requests and sends them one
after another, even right after a colorscheme change when many panes need
updating together. Doing them all at once turned out to occasionally
confuse zellij (a harmless log error about a pane connection that had
already closed), so this trades a tiny bit of speed for not risking that.
With a normal number of panes open you won't notice any delay.

New panes: panes only get recolored when you change colorscheme or start
nvim, so a pane you open afterwards will briefly show zellij's default
background until it catches up. To smooth that over, a quiet background
check runs about once a second (`pane_poll_interval_ms`) looking for
panes it hasn't colored yet. Set `pane_poll_interval_ms = 0` if you'd
rather turn that off.

Getting rid of that brief delay entirely would take a small companion
living inside zellij itself, reacting the moment a pane appears instead
of checking every second or so. Nothing exists for this yet — see the
Roadmap section in the README for where this is headed.

==============================================================================
HEALTH                                                       *zj-theme-health*

Run `:checkhealth zj-theme` to check:

  - whether `setup()` has been called
  - whether you're currently inside a zellij session
  - whether `zellij_config_path` exists and has a `theme "..."` line
  - whether the currently active colorscheme has a mapping
  - whether `sync_pane_backgrounds` is active (|zj-theme-pane-backgrounds|)
  - whether the `zellij` CLI is on `PATH` and its version is new enough
    for `set-pane-color` (only needed for the other-panes part; nvim's own
    pane syncs regardless)

vim:tw=78:ts=8:noet:ft=help:norl:
